// vite.config.ts
    import { defineConfig } from 'vite';

    export default defineConfig({
      plugins: [], // Include your framework plugin here
      // Other Vite configuration options can be added here
      // For example, to specify the root directory if it's not the project root:
      // root: 'src', 
    });